# pause-unpause

Extension sources for [pause-unpause](https://github.com/Nitin-kun/pause-unpause).

Install from the repo root with `./install.sh` or `.\install.ps1`. See the [project README](../README.md) for the one-command installers and how to load this folder unpacked in Chrome.
